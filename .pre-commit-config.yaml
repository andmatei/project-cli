# Pre-commit hooks for keel.
# Install once per clone: `pre-commit install`
# Run on all files manually: `pre-commit run --all-files`
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.12
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format
